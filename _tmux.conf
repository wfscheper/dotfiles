run-shell "powerline-daemon -q"
source "$POWERLINE_ROOT/bindings/tmux/powerline.conf"

# utf8 status bar
set-option -g status-utf8 on

##########################
# Default status options #
##########################
# set -g status on
# set -g status-attr none
# set -g status-bg green
# set -g status-fg black
# set -g status-interval 15
# set -g status-justify left
# set -g status-keys vi
# set -g status-left "[#S]"
# set -g status-left-attr none
# set -g status-left-bg default
# set -g status-left-fg default
# set -g status-left-length 10
# set -g status-left-style default
# set -g status-position bottom
# set -g status-right ""#{=22:pane_title}" %H:%M %d-%b-%y"
# set -g status-right-attr none
# set -g status-right-bg default
# set -g status-right-fg default
# set -g status-right-length 40
# set -g status-right-style default
# set -g status-style fg=black,bg=green
# set -g status-utf8 on
# setw -g window-status-activity-attr reverse
# setw -g window-status-activity-bg default
# setw -g window-status-activity-fg default
# setw -g window-status-activity-style reverse
# setw -g window-status-attr none
# setw -g window-status-bell-attr reverse
# setw -g window-status-bell-bg default
# setw -g window-status-bell-fg default
# setw -g window-status-bell-style reverse
# setw -g window-status-bg default
# setw -g window-status-content-attr reverse
# setw -g window-status-content-bg default
# setw -g window-status-content-fg default
# setw -g window-status-content-style reverse
# setw -g window-status-current-attr none
# setw -g window-status-current-bg default
# setw -g window-status-current-fg default
# setw -g window-status-current-format "#I:#W#F"
# setw -g window-status-current-style default
# setw -g window-status-fg default
# setw -g window-status-format "#I:#W#F"
# setw -g window-status-last-attr none
# setw -g window-status-last-bg default
# setw -g window-status-last-fg default
# setw -g window-status-last-style default
# setw -g window-status-separator " "
# setw -g window-status-style default


#########################
# Custom status options #
#########################
#set -g status-position bottom
#set -g status-fg colour240
#set -g status-bg colour236
#set -g status-left '#{?client_prefix,#[fg=colour234]#[bg=colour188],#[fg=colour188]#[bg=colour234]} #S #{?client_prefix,#[fg=colour188],#[fg=colour234]}#[bg=colour236]'
#set -g status-right '#[fg=colour242] %a %m/%d/%y #[fg=colour235,bg=colour236]#[fg=colour188,bg=colour235] %H:%M %Z #[fg=colour234,bg=colour235]#(test -z "$SSH_TTY" || echo "#[fg=colour166]")#[fg=colour188,bg=colour234]#(test -z "$SSH_TTY" || echo "#[fg=colour232,bg=colour166]") #(whoami)@#h #(test -z "$SSH_TTY" || echo " ")'
#set -g status-right-length 100
#set -g status-left-length 100

#setw -g window-status-current-bg colour24
#setw -g window-status-current-format '#[fg=colour236] #[fg=colour247]#I #[fg=colour188,bold] #W #[fg=colour24,bg=colour236]'

#setw -g window-status-format ' #[fg=colour238]#I #[fg=default] #W '

#setw -g window-status-activity-attr none
#setw -g window-status-activity-bg default
#setw -g window-status-activity-fg colour209
#setw -g window-status-attr none
#setw -g window-status-bell-attr none
#setw -g window-status-bell-bg default
#setw -g window-status-bell-fg colour209

# set the base pane number to 1
set-option -g base-index 1

# set up rate limitting
set -g c0-change-trigger 250
set -g c0-change-interval 100

# set default shell
set-option -g default-shell /bin/bash
set-option -g default-terminal screen-256color

# C-b is not acceptable -- Vim uses it
unbind C-b
set -g prefix C-Space
bind a send-prefix

# Allows for faster key repetition
set -s escape-time 0

# Rather than constraining window size to the maximum size of any client 
# connected to the *session*, constrain window size to the maximum size of any 
# client connected to *that window*. Much more reasonable.
setw -g aggressive-resize on

# Activity monitoring
setw -g monitor-activity on
set -g visual-activity on

# copy & paste using vim-style bindings
set -g mode-keys vi
unbind [
unbind ]
bind C-c copy-mode
bind C-v paste-buffer
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection

# quick view of processes
bind '~' split-window "exec htop"

# hjkl pane traversal
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind b last-window

# History
set -g history-limit 10000

# more intuitive pane creation
unbind '"'
unbind %
bind L split-window -h
bind J split-window -v

# reload config
bind r source-file ~/.tmux.conf \; display-message "Config reloaded..."

# auto window rename
set-window-option -g automatic-rename on

# rm mouse mode fail
set -g mode-mouse off
set -g mouse-select-pane off
set -g mouse-select-window off
set -g mouse-resize-pane off
